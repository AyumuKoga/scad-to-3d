# 限定提供前の条件

共通パスワードを知るユーザー向けに限定提供する。このアプリの運用費用は0円を必須条件とする。ユーザーの外部公開指示に基づきCloudflare Pagesへ配置済み。公開先は https://scad-to-3d-private.pages.dev/ 。

## 認証と費用（配布前の必須条件）

- [x] 共通パスワードの自動生成をユーザーが選択。
- [x] サーバー側の全パス認証、Secret不備・エラー時の拒否を実装。
- [x] Chromium/WebKitで未認証の拒否と認証後の実WASM生成・STL保存を検証。
- [x] Cloudflare Workers Free / $0 / Current planを管理画面で確認。
- [x] 本番・プレビューのSecretを設定し、両方をFail closedにした（APIで確認）。
- [x] 本番・ハッシュURLで認証とファイル保護を確認。ブランチエイリアス・プレビューデプロイは存在しない。
- [x] 無料枠超過時の提供停止を設定。有料プラン・API・独自ドメインの購入なし。

詳細は [限定提供の手順](private-deployment.md) を参照。

## ライセンスと配布

- [x] 実装前にGPLバイナリ配信のソース提供義務を明示。
- [x] アプリLICENSE、OpenSCAD COPYING、Three.jsほか直接依存のライセンス本文へのリンクを同梱。
- [x] 固定WASMの取得URLとSHA-256をビルドスクリプトに記録し、検証後のみ利用。
- [ ] この公式WASMビルドに対応する正確なOpenSCADソースrevision、WASM用パッチ、依存ソース、ビルドスクリプト・toolchain情報を取得。
- [ ] 実際にリンクされたOpenSCAD依存ライブラリのバージョン・個別ライセンスをそのビルドに照合し、必要な表示を追加。
- [ ] アプリのソース、対応エンジンソース一式、依存ライセンスとビルド手順を同じ配布場所等から利用者が取得できるようにし、licenses.htmlの案内を実URLへ更新。
- [ ] 残件：上流の全依存ソースの照合、または固定ソースからWASMを再ビルドして同時提供。

本アプリのソース一式とOpenSCAD本体の対応revisionを配布場所へ追加した。完全な対応ソース一式の確定は未完了で、[来歴と調査状況](engine-provenance.md)に記録。

公式のZIPはJSとWASMのみ。npmのmetadataやGitHubの現在のmasterだけでは、対応ソース一式を確定できないため、この条件を未完了として記録している。教育利用だから免除されるとは扱わない。アプリ新規部分はGPL-2.0-or-laterで提供する。

## 実環境

- [x] ChromiumとWebKitのブラウザ自動テスト。
- [x] Mac上のBambu Studio CLIでブラケット・スマホスタンド・ドリンクホルダーがmanifold=yes、1 partと確認。
- [ ] Windows実機のChrome / Edgeで貼付・生成・回転・ズーム・パン・ダウンロード。
- [ ] macOSのSafari実製品で同じ操作。Playwright WebKitとは別に確認。
- [ ] 学校のiPad実機Safariで1本指・ピンチ・2本指パン、ファイルAppへの保存、横/縦画面切替、メモリ負荷。
- [ ] 必要ならPrusaSlicer / CuraでSTLを開く（本作業では未実施）。
- [ ] 学校回線で40人分の初回配信と端末性能を確認。コード生成は各端末なのでサーバーのCPU負荷は増えないが、初回静的asset転送は人数に比例する。
- [ ] 静的ホストのMIME・HTTPS・圧縮・キャッシュ・CSP・無料枠を確認。

## メモリに関する残存リスク

配布版のheap最大値は約4GB。60秒のWorker終了は暴走時間を抑えるが、短時間のメモリ急増やブラウザタブの終了まで保証するものではない。学校用途で厳密な上限が必要な場合、たとえば512MBの `MAXIMUM_MEMORY` を設定したエンジンをビルドし、対象端末で検証する。JS引数を足すだけで上限を変更できるとは主張しない。
